[project]
name = "recettes-cuisine"
version = "0.1.0"
description = "Build-time card thumbnails for recipe images"
requires-python = ">=3.10"
dependencies = [
    "pillow>=10",
    "pyyaml>=6",
    "requests>=2.31",
    "websocket-client>=1.7",
]

